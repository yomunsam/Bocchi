# Themes/

This directory is reserved for repository-shipped or example Themes (e.g., the default SvelteKit theme to be implemented in M5).

It is **not** the same as a user workspace's `themes/` directory. User workspaces keep their own theme copies; this folder only contains themes that ship with the Bocchi repository itself.

In M1 this directory is intentionally empty.
