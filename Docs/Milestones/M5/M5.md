# M5 Default SvelteKit Theme

本文件是 M5 阶段的“作战图”。它把 `Docs/Milestones.md` 中 M5 的“Default SvelteKit Theme”拆成可实现、可验收、可继续讨论视觉的具体任务，并把默认前台 Theme 的架构边界、默认视觉方向、Theme 配置、静态路由、构建链路和验证口径固定下来。

> 上下文恢复入口：阅读顺序为 `Docs/Architecture.md` → `Docs/Milestones.md` → `Docs/Milestones/M3/M3.md` → `Docs/Milestones/M4/M4.md` → 本文件。
>
> M5 以后，Bocchi 第一次拥有真正可用的前台站点 Theme。Dashboard 的柔和 App 风格仍以 `Docs/Milestones/M4/UI-Design.md` 为准；本文件只约束对外前台页面与 Theme Contract。

## 1. 目标

让 Bocchi 从“能生成 Theme 输入数据”进入“能生成一套真实可部署个人主页”的阶段：

1. 在 `Themes/default-svelte/` 建立仓库内置默认 Theme，Theme id 固定为 `default-svelte`。
2. 使用 SvelteKit + static adapter 实现完全静态输出，不引入公网动态运行时。
3. 消费 M3 已产出的 `.bocchi/input/*.json`，渲染首页、文章、页面、作品、短文和友链。
4. 固化一套默认前台视觉：克制现代、排版优先、黑白纸墨为主、少量焦橙 accent、网格清晰、响应式友好。
5. 补齐 Theme 运行链路中 M5 必需的缺口：默认 Theme 安装/物化、`theme-config.json`、Theme 输出收集、manifest 对账和构建错误呈现。
6. 保证 Home Server `/` Preview Host 可以预览默认 Theme，Preview Toolbar 注入不需要 Theme 自己实现。

## 2. 假设与边界

### 2.1 当前假设

- M5 不改变 Home Server / Generator / Theme Contract 的大方向：Home Server 只生成标准输入、执行 Theme 构建、校验输出，不理解 SvelteKit 内部组件。
- 默认 Theme 的实现源码放在仓库 `Themes/default-svelte/`，运行时工作区仍从 `<workspace>/themes/default-svelte/` 加载 Theme。
- 默认 Theme 可以被复制到工作区系统空间；内容空间 `content/` 仍不包含 Theme 实现、Theme 配置或构建产物。
- M5 只支持单语言内容。语言切换与多语言路由需要新的内容模型，不在默认 Theme 里假装实现。
- M5 继续把 RSS / Atom、Sitemap、robots 视为 Generator 产物；Theme 只链接它们，不重新生成。

### 2.2 视觉参考边界

默认 Theme 可以吸收 mono 原型的这些方向：

- editorial / personal portfolio 的内容气质。
- 8px grid、强分隔线、克制留白、排版做主角。
- ink / paper 中性色为主，焦橙只做少量链接、hover 和 active 状态。
- 文章列表采用可扫读的行式列表；作品页才使用少量边线卡片。
- 默认无复杂动效，只有 hover、focus 和很轻的进入反馈。

默认 Theme 不照搬这些内容：

- 不复用静态原型代码、页面文案、角色图形或外部资源路径。
- 不把原型里的账号页、后台页、评论区、点赞、收藏、搜索按钮当作 M5 功能。
- 不使用负 `letter-spacing`，也不使用随 viewport 连续缩放的字体尺寸；字号用语义 token 和断点调整。
- 不添加吉祥物、角色、可识别动漫元素或装饰性 emoji。Bocchi 前台默认 Theme 应该靠内容、排版和用户媒体建立气质。

## 3. 关键结论

### 3.1 默认 Theme 名称

- Theme id：`default-svelte`
- 显示名称：`Bocchi Mono`
- 版本：M5 初始为 `0.1.0`
- Contract：`1.0`

`Bocchi Mono` 是默认视觉名称，不改变 Theme Contract id。这样 Home Server、site.yaml 和构建配置继续使用稳定 id，而 UI 可以显示更自然的主题名称。

### 3.2 架构优先级

M5 不能只做一个 SvelteKit 项目。真正的完成标准是：Generator 能把默认 Theme 的输出纳入同一份 `.bocchi-manifest.json`，Home Server Preview 能打开生成后的页面，Dashboard 里选择默认 Theme 后能实际影响构建。

因此 M5 的前置工程任务是：

1. **默认 Theme 物化**：仓库内置 `Themes/default-svelte/` 是源；工作区 `<workspace>/themes/default-svelte/` 是运行实例。初始化或明确安装动作只在目标不存在时复制，不覆盖用户修改。
2. **Theme 配置输入**：生成 `.bocchi/input/theme-config.json`，来源为 `.bocchi/theme-config/{themeId}.json`；Home Server 的 `ThemeConfigurationRecord` 作为 UI 投影，需要在保存时同步到该文件。
3. **Theme 输出收集**：Theme 先输出到 `theme.json.outputDir` 指定的 Theme 本地目录，再由 Generator 新增的输出收集阶段写入 `output/public/` 并登记为 `ArtifactKind.ThemeOutput`。
4. **manifest 对账**：`WriteManifestStage` 必须包含 Theme HTML / CSS / JS / assets，否则 M6 发布无法可靠对账。

这比让 SvelteKit 直接写入 `output/public/` 更清晰。直接写入会绕过 `BuildArtifact`，随后被 `ValidateOutputStage` 当作未登记 orphan 文件清理，和现有 artifact 模型冲突。

### 3.3 功能不伪装

M5 不做还没有数据支持的交互：

- 不显示搜索入口；搜索索引属于 M6。
- 不显示评论、点赞、收藏、阅读量；这些需要 Cloud Server 或第三方动态能力。
- 不显示语言切换；当前内容模型没有多语言字段。
- 不显示不可用按钮、占位设置或“以后会做”的控件。

## 4. 交付范围

### 4.1 在范围内

- `Themes/default-svelte/`：
  - `theme.json`
  - `config-schema.json`
  - `package.json`
  - SvelteKit static 项目结构
  - Theme 输入读取层
  - 全局 layout、页面组件、基础样式和小型前端脚本
- 默认页面：
  - 首页 `/`
  - 文章列表 `/posts/`
  - 文章详情 `/posts/{year}/{slug}/`
  - 独立页面 `/{slug}/`
  - 作品列表 `/works/`
  - 作品详情 `/works/{year}/{slug}/`
  - 短文总览 `/notes/`
  - 短文年份页 `/notes/{year}/`
  - 友链 `/friends/`
  - `404.html`
- Generator / Workspace 集成：
  - 默认 Theme 物化或安装路径
  - `theme-config.json` 写入
  - Theme 本地输出目录清理
  - Theme 输出收集为 `ArtifactKind.ThemeOutput`
  - manifest 和 validate 与 Theme 输出一致
- Home Server 集成：
  - Setup / Workspace 初始化后默认主题可用
  - Settings 中默认 Theme 配置保存后参与构建
  - Build / Preview 页面可以跑通默认 Theme
- 验证：
  - .NET 单元/集成测试覆盖新增 Generator 行为
  - Theme 构建 smoke 覆盖空内容、完整样例内容、草稿预览
  - 关键视口截图或手工验收记录

### 4.2 非目标

- 不做多套视觉主题。
- 不做 Theme marketplace。
- 不做 Theme 热更新、远程下载或不可信沙箱。
- 不做搜索索引生成和搜索 UI。
- 不做评论、点赞、收藏、访问统计。
- 不做图片派生、响应式图片生成或 OG image 生成。
- 不做多语言内容模型。
- 不做细粒度增量渲染；M5 仍然允许整站 Theme build。

## 5. Theme Contract 与构建链路

### 5.1 目录结构

仓库内置源：

```text
Themes/default-svelte/
  theme.json
  config-schema.json
  package.json
  pnpm-lock.yaml
  svelte.config.js
  vite.config.ts
  tsconfig.json
  src/
    app.html
    lib/
      bocchi/
        inputs.ts
        routes.ts
        theme-config.ts
      components/
      styles/
    routes/
      +layout.svelte
      +page.svelte
      posts/
      works/
      notes/
      friends/
      [slug]/
```

工作区运行实例：

```text
<workspace>/
  themes/
    default-svelte/
      theme.json
      config-schema.json
      package.json
      ...
  .bocchi/
    input/
      site.json
      navigation.json
      posts.json
      pages.json
      works.json
      notes.json
      friends.json
      photos.json
      theme-config.json
      build-context.json
    theme-config/
      default-svelte.json
  output/
    public/
      index.html
      posts/
      works/
      notes/
      friends/
      media/
      .bocchi-manifest.json
```

### 5.2 theme.json

`theme.json` 初始建议：

```json
{
  "id": "default-svelte",
  "name": "Bocchi Mono",
  "version": "0.1.0",
  "contractVersion": "1.0",
  "inputDir": ".bocchi/input",
  "outputDir": "build",
  "build": {
    "command": "pnpm build",
    "installCommand": "pnpm install --frozen-lockfile"
  },
  "features": {
    "posts": true,
    "pages": true,
    "works": true,
    "notes": true,
    "friends": true,
    "photos": false,
    "search": false
  }
}
```

`search` 初始为 false。即使 `SiteSettings.EnableSearch` 已有字段，默认 Theme 在 M5 不显示搜索入口，避免用户看到不可用功能。

### 5.3 Theme 输入读取

Theme 读取优先级：

1. `BOCCHI_INPUT_DIR` 环境变量。
2. Theme 根目录下 `theme.json.inputDir`。
3. 开发 fallback：`.bocchi/input`。

读取规则：

- 每个输入文件先解开 `InputEnvelope<T>`。
- `contractVersion` 必须等于 `1.0`，否则构建失败并给出清晰错误。
- `data` 做轻量结构断言，不重复 Generator 已经做过的内容校验。
- 文章、作品、短文的排序在 Theme 侧完成，用 `publishedAt ?? updatedAt` 降序；缺日期的内容排在后面。
- 正文默认使用 `html`，不在 Theme 中二次解析 Markdown。代码高亮、数学公式和图片灯箱以后可以作为 Theme 内部增强，但 M5 不先扩协议。

### 5.4 theme-config.json

M5 正式补齐 `.bocchi/input/theme-config.json`：

```json
{
  "$schema": "https://bocchi.local/schema/v1/theme-config.json",
  "contractVersion": "1.0",
  "generatedAt": "2026-05-15T00:00:00Z",
  "data": {
    "visual": {
      "accentColor": "#E85D3A",
      "paperTone": "warm"
    },
    "home": {
      "featuredPosts": 5,
      "featuredWorks": 4,
      "recentNotes": 3,
      "showFriends": true
    },
    "reading": {
      "showUpdatedAt": true,
      "showReadingTime": false
    }
  }
}
```

配置原则：

- 配置控制“展示策略”，不复制 `site.yaml` 已经拥有的站点事实。
- 颜色只开放 accent，不开放整套 token。默认 Theme 先保持一致审美，不做主题编辑器。
- `reading.showReadingTime` 初始 false，因为当前 Contract 没有字数或阅读时间字段；开启时只能由 Theme 从纯文本估算，验收前要确认是否值得做。

### 5.5 config-schema.json

初始 schema 分三组：

- `visual`：accent color、paper tone、是否显示首页媒体区域。
- `home`：首页文章/作品/短文数量、是否显示友链。
- `reading`：更新时间、目录、阅读时间估算。

Schema 只描述 M5 真能执行的配置；不放搜索、多语言、评论或统计开关。

### 5.6 Theme 依赖安装

M5 不应在每次 Full Build 都运行 install。建议行为：

- `installCommand` 作为显式动作：Dashboard Build / Settings 可以提示“安装或更新默认 Theme 依赖”。
- CLI smoke 和开发验证可以手动运行 `pnpm install --frozen-lockfile`。
- Full Build 发现依赖缺失时给出可执行的错误提示，而不是把 SvelteKit 的长堆栈直接抛给普通用户。
- 后续如果要自动安装，必须用 lockfile hash 做幂等判断，并在 UI 上明确这是执行本机代码。

## 6. 页面与路由设计

### 6.1 首页 `/`

首页不是营销 landing page，而是个人主页入口。首屏应立即表达站点主人、写作方向和最近内容。

结构：

1. 顶栏：wordmark、导航、前台外观按钮；不放搜索。
2. Hero：站点标题、站点描述、作者简介或一句引言。
3. Selected writing：最新或精选文章行式列表。
4. Selected work：精选作品边线卡片。
5. Recent notes：最近短文流。
6. Colophon：站点元信息、RSS / Atom、社交链接。

如果存在 slug 为 `index` 的 Page，可以作为首页正文补充，但不能替代站点标题和内容入口。

### 6.2 文章列表 `/posts/`

默认使用行式列表：

```text
2026 - 05   文章标题                             分类 / tag
2026 - 04   另一篇文章                           分类 / tag
```

桌面端按年/月、标题、分类/标签分列；移动端折成两行，保持触摸区域足够大。

### 6.3 文章详情 `/posts/{year}/{slug}/`

结构：

- 返回 Writing 的轻链接。
- 标题、摘要、发布时间、更新时间、分类、标签。
- 可选 cover，使用稳定 aspect-ratio。
- 正文宽度约 680px。
- 文章尾部显示标签、上一篇/下一篇。

不显示评论、点赞、阅读量、收藏按钮。没有数据来源的社交互动不进入 M5。

### 6.4 独立页面 `/{slug}/`

独立页面服务 About、Uses、Now 等长期页面：

- 使用比文章更简单的页面头。
- 不显示发布时间。
- 支持 `showInNavigation` 进入顶栏。
- 保留正文同样的 prose 样式。

路由冲突处理：

- `/posts/`、`/works/`、`/notes/`、`/friends/` 为保留路径。
- Page slug 不能覆盖保留路径；如果内容层没有阻止，Theme 构建阶段要报清晰错误。

### 6.5 作品列表与详情

`/works/` 使用边线卡片，但不做大投影或复杂网格：

- 标题。
- 一句话 summary。
- period / role。
- stack 文本标签。
- links。
- 可选 cover。

`/works/{year}/{slug}/` 使用作品详情模板，正文仍来自 `html`，上方强化作品元数据。

### 6.6 短文 `/notes/` 与 `/notes/{year}/`

短文总览按年份分组，年份页展示具体条目：

- 时间戳使用 mono 字体。
- 每条短文用正文 + 媒体 + tag + 分隔线，不做卡片墙。
- 短文没有单条详情页，除非后续内容模型显式需要 permalink。

### 6.7 友链 `/friends/`

友链页默认是文本优先的目录：

- name。
- description。
- url。
- tags。
- avatar 可选；没有 avatar 时不能出现破图或占位大图。

友链 status 不是 `published` 时默认不展示，除非 BuildContext `includeDrafts` 为 true。

### 6.8 404

`404.html` 使用同一套顶栏和视觉 token，但保持克制：

- 简短说明。
- 回首页和 Writing 的链接。
- 不做满屏插画或游戏化交互。

## 7. 视觉系统

### 7.1 色彩

语义 token：

| Token | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#FAFAF7` | `#0A0A0B` | 页面背景 |
| `--surface` | `#FFFFFF` | `#141416` | 少量表面 |
| `--surface-soft` | `#F1F1EC` | `#1A1A1C` | hover / 区块 |
| `--text` | `#101012` | `#EDEDEA` | 主文字 |
| `--text-muted` | `#52524F` | `#A8A8A4` | 次要文字 |
| `--text-faint` | `#9A9A95` | `#6A6A66` | 弱信息 |
| `--line` | `#E5E5E0` | `#26262A` | 分隔线 |
| `--accent` | `#E85D3A` | `#FF7A55` | 链接 / active / focus |
| `--accent-soft` | `#FFE9E0` | `#3A1B11` | 极少量提示底色 |

规则：

- accent 面积控制在全站很小比例；首屏不能被焦橙吞掉。
- 默认背景不能变成米色系一把梭；用户图片、cover 和内容媒体应承担大部分视觉变化。
- Dark 是低刺激阅读模式，不是终端主题。

### 7.2 字体与排版

默认字体栈：

```css
--font-display: "Instrument Serif", "Noto Serif SC", ui-serif, Georgia, serif;
--font-body: "Inter Tight", "Inter", "Noto Sans SC", ui-sans-serif, system-ui, sans-serif;
--font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
```

M5 实现时要提供系统字体 fallback。外部字体加载失败时页面仍必须可读。

字号原则：

- Hero / article title 可以使用 display 字体，但只在真正的大标题中使用。
- 列表、卡片、导航、footer 使用 body 或 mono。
- 不使用负 letter-spacing。
- 不用 `vw` 直接连续缩放字体；按断点切换语义字号。

### 7.3 布局

- baseline：8px。
- 顶栏高度：56px。
- 最大容器：1280px。
- 主内容：1080px。
- 正文宽度：680px。
- 卡片圆角最大 8px。
- 页面分隔主要依靠 1px line，不依赖大投影。
- 固定格式元素必须有稳定尺寸，避免 hover、长标题或图标加载导致布局跳动。

### 7.4 响应式

检查视口：

- `1440x900`
- `1024x768`
- `768x1024`
- `412x915`

移动端规则：

- 顶栏导航折叠为菜单。
- 列表行折成两行，不变成压缩表格。
- 作品卡片单列。
- 正文左右 padding 保持可读，长 URL / 长英文单词可换行。
- 前台外观按钮仍是图标按钮，但必须有 accessible label。

### 7.5 动效

- 默认无入场大动画。
- hover / focus / active 使用 120-200ms。
- 尊重 `prefers-reduced-motion`。
- 不做滚动视差、全屏过渡或复杂页面切换。

## 8. 与 Home Server Preview 的关系

Preview Toolbar 仍由 Home Server Preview Host 注入，默认 Theme 不包含后台返回按钮或编辑按钮。

Theme 需要配合的点：

- 顶栏 z-index 不应高到盖住 Preview Toolbar。
- HTML 结构应保留正常 `<main>`，便于注入脚本定位。
- 不用全屏 fixed overlay 作为常规布局。
- 所有页面都使用稳定的 canonical route，便于 `PreviewRouteMapService` 识别内容。

## 9. 实现任务

### 9.1 M5-T01 设计与决策

- [x] 固化本文件。
- [x] 根据本文件更新 `Docs/Milestones.md` 的 M5 入口。
- [x] 明确 M5 不实现搜索、评论、多语言和多视觉主题。

### 9.2 M5-T02 Theme 运行链路补齐

- [ ] 新增默认 Theme 物化/安装服务：仓库源 → 工作区系统空间，已存在时不覆盖。
- [ ] `WorkspaceInitializer` 或 Setup 完成阶段把 `defaultThemeId` 设置为 `default-svelte`。
- [ ] 新增 `.bocchi/theme-config/{themeId}.json` 文件读写边界。
- [ ] `ThemeSettingsService` 保存时同步 Theme 配置文件。
- [ ] `ThemeInputWriter` 或相关 Stage 写出 `theme-config.json`。
- [ ] `RunThemeBuildStage` 把 `BOCCHI_OUTPUT_DIR` 指向 Theme 本地 `outputDir`。
- [ ] 新增 `CollectThemeOutputStage`：扫描 Theme 本地输出，写入 Sink，并登记 `ArtifactKind.ThemeOutput`。
- [ ] `GeneratorPipeline` 顺序调整为 RunThemeBuild → CollectThemeOutput → WriteManifest → ValidateOutput。
- [ ] 新增测试覆盖 Theme 输出进入 manifest、orphan cleanup 不删除 Theme 输出。

### 9.3 M5-T03 SvelteKit Theme 骨架

- [ ] 创建 `Themes/default-svelte/`。
- [ ] 添加 SvelteKit static adapter 配置。
- [ ] 添加 `theme.json`、`config-schema.json`、`package.json`、lockfile。
- [ ] 实现 `src/lib/bocchi/inputs.ts`。
- [ ] 实现 contract version 检查和清晰错误。
- [ ] 实现 Theme config 默认值合并。

### 9.4 M5-T04 页面实现

- [ ] 全局 layout、顶栏、footer、外观切换。
- [ ] 首页。
- [ ] 文章列表与详情。
- [ ] 独立页面。
- [ ] 作品列表与详情。
- [ ] 短文总览与年份页。
- [ ] 友链页。
- [ ] 404 页面。

### 9.5 M5-T05 视觉精修

- [ ] 全局 CSS token。
- [ ] prose 正文样式。
- [ ] 行式列表样式。
- [ ] 作品卡片样式。
- [ ] 媒体/cover 稳定尺寸。
- [ ] Dark mode。
- [ ] focus-visible 与键盘导航。
- [ ] 移动端布局。

### 9.6 M5-T06 验证

- [ ] `pnpm install --frozen-lockfile` in `Themes/default-svelte/`。
- [ ] `pnpm build` in `Themes/default-svelte/` using sample `.bocchi/input/`。
- [ ] `dotnet test Bocchi.slnx`。
- [ ] Home Server Full Build 能输出默认 Theme HTML。
- [ ] `/` Preview Host 能打开默认 Theme 首页。
- [ ] Manifest 包含 ThemeOutput。
- [ ] 截图或手工检查四个关键视口。

## 10. 验收标准

M5 完成时必须满足：

- `default-svelte` 可以被 Home Server 加载并构建。
- `output/public/` 包含完整静态站点页面、媒体、站点产物和 `.bocchi-manifest.json`。
- `.bocchi-manifest.json` 包含 Theme HTML / CSS / JS / assets，不存在未登记输出。
- 首页、文章、页面、作品、短文、友链都有可访问页面。
- 主题配置能影响实际页面，例如 accent color、首页展示数量。
- Dashboard 选择默认 Theme 后，Full Build 和 Preview 使用同一套配置。
- 公开站点页面不出现未实现功能入口。
- Light / Dark 可用，移动端不需要另一个信息架构。
- `dotnet test Bocchi.slnx` 通过；Theme 自身 build 通过。

## 11. 风险与处理

| 风险 | 处理 |
| --- | --- |
| SvelteKit 直接写入 `output/public/` 导致 artifact/manifest 失真 | M5 改为 Theme 本地输出 + `CollectThemeOutputStage` |
| 默认 Theme 只存在仓库内，工作区 build 找不到 | 增加默认 Theme 物化/安装服务 |
| Theme 设置只存在 SQLite，CLI build 读不到 | 以 `.bocchi/theme-config/{themeId}.json` 作为构建源，SQLite 作为 UI 投影 |
| 视觉原型里的不可用功能被搬进默认站点 | M5 明确不显示搜索、评论、点赞、语言切换 |
| 字体或外部 CDN 不可用 | 提供系统字体 fallback；不依赖远端脚本完成首屏 |
| 用户内容为空时页面尴尬 | 首页和列表页提供克制 empty state，提示从 Dashboard 写第一篇内容 |
| Theme 依赖安装失败信息太技术化 | Build 页面将常见缺依赖错误翻译成可执行提示 |

## 12. 开放问题

- 默认 Theme 依赖安装是否要在 Setup 后主动提示，还是只在首次构建失败时提示？
- `reading.showReadingTime` 是否值得在没有字数字段的情况下由 Theme 估算？
- Page slug 与保留路由冲突应在内容扫描阶段报错，还是先由 Theme 构建阶段报错？
- 仓库内置 Theme 到工作区 Theme 的更新策略是否进入 M5，还是等 M6 发布前再设计版本迁移？
