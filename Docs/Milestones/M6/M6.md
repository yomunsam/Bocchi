# M6 Localization and Content i18n

本文件是 M6 阶段的“作战图”。它把 Dashboard 自身 i18n、站点语言设置、前台 Theme 本地化约定、内容多语言版本、URL/SEO 输出和后续翻译能力边界固定下来，确保本次讨论丢失后也能从文档恢复完整设计。

> 上下文恢复入口：阅读顺序为 `Docs/Architecture.md` → `Docs/Milestones.md` → `Docs/Milestones/M4/M4.md` → `Docs/Milestones/M5/M5.md` → 本文件。
>
> M6 以后，Bocchi 开始把“多语言站点”作为产品能力处理。Dashboard 自身的语言切换仍是后台 UI 偏好；前台站点语言、内容语言版本和 Theme 文案覆盖是站点能力，两者必须分开。
>
> Frontend Menu v1 与 Theme Page Contract 的详细实施记录见 `Docs/Milestones/M6/Menu.md`。

## 1. 目标

让 Bocchi 在不打断普通写作流程的前提下，具备清晰、可扩展的本地化能力：

1. Dashboard 自身支持 `zh-CN` 和 `en-US` 两种 UI 语言，并以可扩展到更多语言的选择控件呈现。
2. Dashboard 提供站点本地化设置：主要语言、站点启用语言、自定义语言、通用前台文案覆盖。
3. Theme Contract 能把语言配置、语言元数据、文案覆盖和内容语言版本交给前台 Theme。
4. 默认 Theme `default-static` / `Bocchi Mono` 给出基础示范，首批兼容 `en-US`、`zh-CN`、`zh-TW`、`ja-JP`。
5. Post / Page / Work 支持同一内容的多个语言版本，并能区分“原生语言版本”和“翻译版本”。
6. 静态输出采用稳定的语言路径、`hreflang` 和 canonical 元数据，保证分享、构建、预览和 SEO 行为可预测。
7. 翻译能力先以手动编辑为基础，抽象出 Translation Provider 边界，后续逐步接入浏览器本地翻译、第三方翻译 API 和 LLM API。

## 2. 核心术语

| 术语 | 含义 |
| --- | --- |
| Dashboard UI language | Admin 操作 Dashboard 时使用的界面语言，是后台 UI 偏好，不传给 Theme。 |
| Site primary language | 站点主要语言，用于默认内容语言、无前缀 URL、默认 `<html lang>` 和 fallback 起点。 |
| Site enabled languages | 站点启用语言列表，必须包含主要语言。它表示站点愿意声明和管理的语言集合。 |
| Language record | 一个语言的描述，包含语言代码、当地语言名称和英文名称。 |
| Common i18n key | Bocchi 对前台 Theme 约定的一组通用文案 key，例如 `menu.home`、`common.readMore`。 |
| Theme i18n key | 某个 Theme 在 manifest 中声明的私有文案 key，只对该 Theme 有意义。 |
| Localization group | 同一篇文章、页面或作品在不同语言下的逻辑分组。 |
| Content variant | Localization group 下的一个具体语言版本。 |
| Native variant | 用户直接写作的语言版本，不声明自己翻译自其他版本。 |
| Translation variant | 明确记录自己翻译自同组某个 Native variant 或其他 variant 的语言版本。 |

## 3. 设计边界

- 普通单语言写作流程不出现强制语言步骤。用户新建内容、写 Markdown、保存或发布时，体验应与 M5 单语言站点一致。
- 内容没有显式 `language` 时，系统按 Site primary language 看待它。
- Dashboard UI language 与 Site primary language 是两个独立设置。Admin 可以用中文后台管理英文站点，也可以反过来。
- Dashboard 的语言切换必须支持大于 2 种语言，不使用二态 Toggle。
- Dashboard、Home Server 和默认 Theme 都不显示语言图标或国旗。Theme 可以自行用语言代码关联图标，但这不进入 Bocchi 通用约定。
- 语言代码建议使用 BCP 47 tag，例如 `en-US`、`zh-CN`、`zh-TW`、`ja-JP`。用户自定义语言代码时，Dashboard 给出建议但不强行限制。
- 前台文案覆盖值第一版只支持 plain text。Theme 渲染时按普通文本处理，不把覆盖值当 HTML 或 Markdown。
- M6 的内容多语言模型覆盖 Post / Page / Work。Note 可以携带语言用于未来筛选和搜索，但 M6 不给 Note 建立独立详情页或语言版本编辑流。
- M6 不把 Dashboard 做成专业翻译管理平台。Settings 页面保留个人发布 App 的低到中信息密度，复杂编辑放进可搜索列表、Modal 和右侧小组件。

## 4. Dashboard 自身 i18n

Dashboard 自身 i18n 属于 Home Server UI 能力，不属于前台 Theme Contract。

### 4.1 资源形态

Dashboard 文案使用 JSON 资源文件维护，首批资源：

```text
Src/HomeServer/Bocchi.HomeServer/Localization/Dashboard/
  zh-CN.json
  en-US.json
```

资源 key 使用稳定语义命名，按页面或组件分组：

```json
{
  "nav.overview": "Overview",
  "nav.content": "Content",
  "settings.localization.title": "Localization",
  "settings.localization.primaryLanguage": "Primary language"
}
```

### 4.2 选择与持久化

- Dashboard UI language 的入口放在 Dashboard 顶栏账号菜单或 `Settings / Localization` 中。
- 控件使用紧凑下拉或可搜索选择框，不使用 `中文 / English` Toggle。
- 选择结果优先作为当前 Admin 的 UI 偏好保存；如果实现阶段还没有用户偏好表，可以先使用 ASP.NET Core culture cookie。
- Dashboard UI language 不写入 `theme-context.json`，也不影响前台站点输出。

## 5. 站点语言设置

M6 新增 `Settings / Localization`。`Settings / Theme` 继续只负责当前 Theme、Theme manifest、Theme 私有配置和 Theme 私有 i18n key 覆盖。

### 5.1 设置内容

`Settings / Localization` 包含：

- Dashboard UI language。
- Site primary language。
- Site enabled languages。
- Custom languages。
- Common i18n text overrides。
- Content localization URL policy。

M6 的 URL policy 固定为 `PrimaryUnprefixed`：

```text
Primary language:
  /about/
  /posts/2026/hello-bocchi/

Other enabled languages:
  /zh-TW/about/
  /zh-TW/posts/2026/hello-bocchi/
  /ja-JP/about/
```

### 5.2 Language record

后台描述语言时使用统一结构：

```json
{
  "code": "zh-CN",
  "nativeName": "简体中文",
  "englishName": "Simplified Chinese"
}
```

内置语言列表写在代码中，覆盖常见语言。用户自定义语言存入 Home Server 数据库，并在 Picklist 中与内置语言一起展示。

### 5.3 Language Picklist

Dashboard 提供可复用 Picklist 组件：

- 可选择、可搜索、可键盘操作。
- 搜索匹配 `code`、`nativeName`、`englishName`。
- 列表项同时展示当地语言名称和英文名称，例如 `简体中文 / Simplified Chinese`。
- 添加启用语言时不能重复选择已有语言。
- Site enabled languages 必须包含 Site primary language；当主要语言变更时，Dashboard 自动确保它在启用列表中。

### 5.4 自定义语言管理

用户可以添加自定义语言：

- `code` 必填。
- `nativeName` 必填。
- `englishName` 必填。
- Dashboard 提示建议使用 BCP 47 tag。
- Dashboard 不因语言代码不在内置列表中而拒绝保存。

这些自定义语言原样传递给 Theme。Theme 是否支持对应语言，由 Theme 自己决定。

## 6. 前台 i18n key 约定

前台 i18n key 是站点能力，不是 Dashboard 自身文案。

### 6.1 Common i18n key

Bocchi 提供一组通用 key 约定，Dashboard 默认内置 key 列表和默认值。Theme 可以使用这些 key，也可以不用。

初始 key 分组：

```text
menu.home
menu.posts
menu.works
menu.notes
menu.friends
menu.about
common.readMore
common.backHome
common.previous
common.next
content.translationNotice
content.viewOriginal
```

Dashboard 的覆盖列表默认为空。用户需要覆盖某个 key 时，新增一个覆盖项：

- 通过可搜索下拉选择内置 key，或输入自定义 key。
- 展开后显示 Site enabled languages 对应的文本输入。
- 未填写的语言回退到 Theme 或 Bocchi 默认值。
- 只保存用户实际填写的覆盖值。

### 6.2 Theme i18n key

Theme 可以在 manifest 中声明自己的私有 i18n key：

```json
{
  "i18n": {
    "supportedLanguages": ["en-US", "zh-CN", "zh-TW", "ja-JP"],
    "keys": [
      {
        "key": "theme.defaultStatic.colophonBuiltWith",
        "title": "Colophon built-with text",
        "description": "Footer text used by Bocchi Mono.",
        "defaultValues": {
          "en-US": "Built with Bocchi.",
          "zh-CN": "由 Bocchi 构建。"
        }
      }
    ]
  }
}
```

Dashboard 在 `Settings / Theme` 中展示当前 Theme 声明的私有 key，并允许 Admin 覆盖。Theme 私有覆盖按 `themeId + key + language` 保存，切换 Theme 后不混用。

### 6.3 覆盖解析顺序

前台 Theme 解析文案时按以下顺序取值：

1. 当前 Theme 的用户覆盖值。
2. Common key 的用户覆盖值。
3. 当前 Theme manifest 中的默认值。
4. Bocchi 通用 key 默认值。
5. key 本身。

语言回退顺序：

1. 当前页面语言。
2. Site primary language。
3. Theme manifest 的默认语言。
4. key 本身。

### 6.4 显示值引用

需要在单一字符串字段中表达“这个展示值应进入前台 i18n 解析”时，使用显示值引用：

```text
i18n://common@menu.home
i18n://theme@theme.defaultStatic.pageTemplate.normal
```

规则：

- `i18n://` 是通用前缀，表示该字段是显示值引用而不是最终展示文本。
- `common@` 指向 Bocchi 通用前台文案 key。
- `theme@` 指向当前 Theme manifest 提供的私有文案 key。
- 本轮只把该约定用于 Dashboard 展示、Theme manifest 的 Page template / special page `displayName` 和 Menu label，不做全字段全局改造。
- 解析不到时，Dashboard 保留原始 key 或 name 作为 fallback，不丢弃用户已保存的值。

## 7. Theme Contract 扩展

M6 在 `cache/theme-input/theme-context.json` 中增加 `localization` 节点：

```json
{
  "data": {
    "site": {
      "language": "en-US"
    },
    "localization": {
      "primaryLanguage": "en-US",
      "enabledLanguages": [
        {
          "code": "en-US",
          "nativeName": "English",
          "englishName": "English"
        },
        {
          "code": "zh-TW",
          "nativeName": "繁體中文",
          "englishName": "Traditional Chinese"
        }
      ],
      "urlPolicy": "PrimaryUnprefixed",
      "text": {
        "menu.home": {
          "en-US": "Home",
          "zh-TW": "首頁"
        }
      }
    }
  }
}
```

内容输入模型为 Post / Page / Work 增加语言字段和 localization 字段：

```json
{
  "id": "posts/2026/hello-bocchi@zh-TW",
  "slug": "hello-bocchi",
  "language": "zh-TW",
  "siteRelativeUrl": "/zh-TW/posts/2026/hello-bocchi/",
  "localization": {
    "groupId": "posts/2026/hello-bocchi",
    "isTranslation": true,
    "sourceLanguage": "en-US",
    "sourceContentId": "posts/2026/hello-bocchi@en-US",
    "alternates": [
      {
        "language": "en-US",
        "url": "/posts/2026/hello-bocchi/",
        "title": "Hello Bocchi"
      },
      {
        "language": "zh-TW",
        "url": "/zh-TW/posts/2026/hello-bocchi/",
        "title": "Hello Bocchi"
      }
    ]
  }
}
```

Theme 可以只使用 `siteRelativeUrl` 和 `alternates`，不需要自己推导 URL。

M6 同时扩展 Theme manifest 的 Page contract：

- `pageTemplates[]` 使用 `{ name, displayName }`，用于声明可编辑 Page 可选择的模板。`normal` 强制存在；Theme 未声明时 Home Server 合成 fallback。
- `specialPages[]` 使用 `{ name, displayName, route }`，用于声明 Theme 自己提供、无需用户编辑 Markdown 的特殊页面。`route` 必须是站点根相对路径。
- Page frontmatter 只保存 `template` name，不保存 Theme id 或关联记录；切换 Theme 后，如果当前 template 不可用，Dashboard 仍显示原 name 并给出警告。
- Menu 可以指向 Theme special page；Home Server 只保存 target，Generator 只在 target 可解析时把它写入公开 Theme input。

## 8. 内容本地化模型

### 8.1 内容文件约定

Post / Page / Work 保留 M2 的目录形式，并强制同一个 localization group 放在同一个内容目录下。语言变体使用 `index.{language}.md` 文件名表达，避免在年份目录或 `pages/` 根目录下把同一内容的不同语言散成多个 sibling 目录。

```text
workspace/
  posts/
    2026/
      hello-bocchi/
        index.md
        index.zh-TW.md
        index.ja-JP.md
        assets/
          cover.jpg
  pages/
    about/
      index.md
      index.zh-CN.md
```

`index.md` 是默认变体。没有显式 `language` 时，它使用 Site primary language。

语言变体 frontmatter 示例：

```yaml
---
title: "Hello Bocchi"
slug: "hello-bocchi"
language: "zh-TW"
localization:
  group: "posts/2026/hello-bocchi"
  translationOf:
    language: "en-US"
---
```

规则：

- `language` 可省略，省略时等于 Site primary language。
- `localization.group` 可省略，省略时由内容类型、年份和目录 slug 推导。
- `localization.translationOf` 存在时，该 variant 是 Translation variant。
- 同一个 group 内，同一种 language 只能有一个 variant。
- Route 使用 group 的 canonical slug，不使用翻译标题推导路径。
- Variant 之间共享同一内容目录下的 `assets/`，避免翻译版本复制媒体。
- Dashboard 新增语言版本时，只在当前 group 目录下创建 `index.{language}.md`，不在年份目录下创建新的并列内容目录。

### 8.2 数据层语义

内容扫描后形成：

```text
ContentLocalizationGroup
- groupId
- contentType
- canonicalSlug
- canonicalRouteBase

ContentVariant
- contentId
- groupId
- language
- title
- sourceRelativePath
- siteRelativeUrl
- isTranslation
- sourceLanguage?
- sourceContentId?
```

`contentId` 建议使用 `groupId@language`。例如：

```text
posts/2026/hello-bocchi@en-US
posts/2026/hello-bocchi@zh-TW
pages/about@ja-JP
```

现有 `ContentItems` 的唯一约束需要从 `Kind + ContentId` 过渡到能够表达 language variant 的形态；旧的 slug 唯一校验也需要改为 `groupId + language` 唯一。

### 8.3 原生版本与翻译版本

- Native variant 是同一 group 下平等的语言版本。
- Translation variant 记录 `sourceLanguage` 和 `sourceContentId`。
- Theme 可以基于 `isTranslation`、`sourceLanguage` 和 `alternates` 显示“当前页面由 English 翻译，查看原文”。
- Translation variant 仍然是独立 Markdown 文件，用户可以自由编辑。

### 8.4 删除与提升

Dashboard 删除内容时遵循：

- 删除普通 variant 时，只删除当前语言文件。
- 删除某个 source variant 时，如果存在 Translation variant，必须显示确认提示。
- 删除 group 中最后一个 Native variant 时，Dashboard 提示这会删除整个 localization group 及其 Translation variants。
- Dashboard 提供“提升为原生版本”动作，把某个 Translation variant 转为 Native variant。

文件删除必须由用户确认；Dashboard 不静默级联删除 Markdown 文件。

## 9. 编辑体验

默认编辑体验保持单语言：

- 新建文章、页面或作品时不显示语言步骤。
- 编辑器右侧可以显示轻量 `Language & versions` 小组件，但不强制用户交互。
- 当前内容未显式语言时，小组件显示它使用 Site primary language。

### 9.1 添加语言版本

入口按钮：`添加语言版本`。

点击后，如果当前内容有未保存修改：

- 提醒用户先保存。
- 告知当前内容会成为这个 localization group 的基础版本。

Modal 字段：

- Language：从 Site enabled languages 中选择，不能选择当前 group 已存在的语言。
- `复制当前内容作为初始内容`：默认开启。
- `这是翻译版本`：默认关闭。
- `翻译来源`：仅在勾选翻译版本时出现，选择同 group 下已有 variant，展示语言和标题。

确认后：

- Dashboard 创建新的 `index.{language}.md`。
- 写入 `language` 和必要的 `localization` frontmatter。
- 跳转到新 variant 的独立编辑界面。

### 9.2 右侧小组件

`Language & versions` 小组件显示：

- 当前语言。
- 当前 variant 是 Native 还是 Translation。
- 同 group 已有语言版本。
- 添加语言版本按钮。
- 如果是 Translation variant，显示来源语言和“查看原文 / 打开来源”入口。

该小组件不使用宽表格，移动端进入折叠区域或独立设置抽屉。

## 10. URL、SEO 与构建输出

M6 固定 `PrimaryUnprefixed` 输出策略。

示例：Site primary language 为 `en-US`，启用 `zh-TW` 和 `ja-JP`。

```text
en-US page:
  /about/
  /posts/2026/hello-bocchi/

zh-TW page:
  /zh-TW/about/
  /zh-TW/posts/2026/hello-bocchi/

ja-JP page:
  /ja-JP/about/
  /ja-JP/posts/2026/hello-bocchi/
```

每个语言页面输出：

```html
<html lang="zh-TW">
<link rel="canonical" href="https://example.com/zh-TW/about/">
<link rel="alternate" hreflang="en-US" href="https://example.com/about/">
<link rel="alternate" hreflang="zh-TW" href="https://example.com/zh-TW/about/">
<link rel="alternate" hreflang="ja-JP" href="https://example.com/ja-JP/about/">
```

规则：

- canonical 指向当前语言页面自身。
- alternate 只列出当前 group 实际存在的语言版本。
- 默认 Theme 提供语言切换入口，但不根据浏览器语言强制跳转。
- Preview Route Map 需要识别带语言前缀的 route，并跳转到对应 variant 编辑页。
- Sitemap 包含所有已发布语言版本。
- Atom / RSS 在 M7 Feeds/Search/Publish 阶段处理语言过滤和多语言 feed 策略。

## 11. 默认 Theme 示范

`default-static` / `Bocchi Mono` 在 M6 提供基础本地化示范：

- manifest 声明支持 `en-US`、`zh-CN`、`zh-TW`、`ja-JP`。
- 使用 `theme-context.json` 中的 `localization` 和 content `alternates`。
- 在有多个语言版本时显示语言切换。
- 在 Translation variant 上显示来源提示。
- 输出 `<html lang>`、canonical、`hreflang`。
- 不展示语言图标或国旗。
- 对未知自定义语言，优先显示 Dashboard 传入的 `nativeName` / `englishName` / `code`。

默认 Theme 的视觉仍遵循 M5 `Bocchi Mono` 方向，不混入 M4 Dashboard 的粉蓝 App 风格。

## 12. Translation Provider

M6 以手动编辑作为基础实现，并设计 Translation Provider 抽象：

```text
TranslationProvider
- providerId
- displayName
- executionContext: browser | server
- availability
- supportedLanguagePairs?
- translate(request)
```

Provider 展示位置：

- 不放在“添加语言版本”Modal 的主路径里。
- 放在新 variant 编辑界面的右侧小组件中。
- 只有当前环境可用时才显示对应 Provider。

演进顺序：

1. 手动编辑：用户自己编写翻译内容。
2. 浏览器本地翻译：检测到浏览器能力时显示，不要求用户配置。
3. 第三方翻译 API：需要用户配置 API Key。
4. LLM API：需要用户配置 API Key 和模型参数。

M6 至少完成抽象和手动编辑闭环；浏览器本地翻译可以作为增强项进入，但必须通过 feature detection 控制 UI 可见性。

## 13. 实施任务

| 状态 | ID | 任务 | 验收 |
| --- | --- | --- | --- |
| [x] | M6-T01 | Dashboard i18n 基线 | Dashboard 可在 `zh-CN` / `en-US` 间切换，资源来自 JSON，选择控件支持未来更多语言。 |
| [x] | M6-T02 | Localization 设置页 | `Settings / Localization` 可配置 Dashboard UI language、Site primary language、Site enabled languages 和自定义语言。 |
| [x] | M6-T03 | 前台 i18n key 覆盖 | 可管理 Common key 覆盖；Theme manifest 可声明私有 key；覆盖进入 `theme-context.json`。 |
| [ ] | M6-T04 | 内容语言模型 | Post / Page / Work loader、scanner、state store、content graph 支持 `language`、group 和 variant。 |
| [ ] | M6-T05 | 编辑器语言版本 UX | 编辑器可添加语言版本、复制基础内容、标记翻译来源，并跳转到独立 variant 编辑。 |
| [ ] | M6-T06 | URL 与 SEO 输出 | Generator 输出语言前缀 URL、`html lang`、canonical、`hreflang`、Sitemap 多语言条目。 |
| [ ] | M6-T07 | 默认 Theme 本地化示范 | `default-static` 支持语言切换、翻译提示和首批四种语言的基础文案。 |
| [ ] | M6-T08 | Translation Provider 抽象 | 右侧小组件预留 Provider 入口，手动编辑闭环稳定，浏览器 Provider 仅在检测可用时出现。 |
| [ ] | M6-T09 | 测试与文档回写 | 单元测试、构建 smoke、Preview Route Map、桌面/移动设置页检查和文档记录完成。 |
| [x] | M6-T10 | Frontend Menu v1 与 Theme Page Contract | `/Admin/Site/Navigation` 可编辑 primary menu tree；Theme 可声明 Page templates 和 special pages；Generator 输出 `navigation.json` 与 `post-categories.json`；默认 Theme 消费 Menu input。 |

## 14. 验收标准

- Dashboard 自身 UI 可切换 `zh-CN` / `en-US`，且控件形态支持更多语言。
- Site primary language 与 Site enabled languages 可配置并持久化。
- Dashboard 可新增自定义语言，并在 Picklist 中按 code/nativeName/englishName 搜索。
- Common i18n key 和当前 Theme 私有 key 可被覆盖，覆盖值进入 Theme Context。
- 普通新建和编辑内容时不需要理解多语言概念。
- Post / Page / Work 可添加另一个语言版本，并生成独立 Markdown variant。
- Translation variant 能记录来源语言和来源内容。
- 构建输出主语言无前缀 URL，其他语言使用语言前缀 URL。
- 默认 Theme 输出语言切换、翻译提示、`html lang`、canonical 和 `hreflang`。
- Preview Host 能从带语言前缀的前台 route 跳转到正确的编辑页面。
- 四个关键视口检查通过：`1440x900`、`1024x768`、`768x1024`、`412x915`。

## 15. 暂不做

- 专业翻译管理平台式的批量矩阵编辑。
- HTML / Markdown / rich text 形式的 i18n 覆盖值。
- 语言图标、国旗或地区象征。
- Note 的独立语言版本详情页。
- 自动重写浏览器语言到另一种语言 URL。
- 第三方翻译 API 和 LLM API 的完整配置界面。
- 多租户、多站点语言策略。

## 16. 决策记录

### 2026-05-16

- Dashboard 自身 i18n 与前台站点本地化分开设计：前者是后台 UI 偏好，后者是站点能力和 Theme Contract 输入。
- 站点语言使用 Site primary language + Site enabled languages 表达；Site enabled languages 必须包含 Site primary language。
- Language record 只包含 `code`、`nativeName`、`englishName`；语言图标不进入 Home Server / Dashboard 通用模型。
- 用户可以添加自定义语言。Dashboard 提示 BCP 47 规范，但不强制限制语言代码。
- 前台 i18n key 分为 Bocchi Common key 和 Theme 私有 key。Theme 私有覆盖按 theme id 隔离保存。
- i18n 覆盖值第一版只支持 plain text。
- Post / Page / Work 通过 localization group + content variant 表达多语言内容；不靠标题或文件名猜测语言关系。
- 翻译版是独立 Markdown 文件，同时记录来源语言和来源内容；删除最后一个 Native variant 前必须提示会影响整个 group。
- M6 固定 `PrimaryUnprefixed` URL 策略：主语言无前缀，其他语言使用语言前缀。
- 默认 Theme 首批示范 `en-US`、`zh-CN`、`zh-TW`、`ja-JP`，并输出语言切换、翻译提示、canonical 和 `hreflang`。

### 2026-05-23

- 前台 Menu 固定为单个站点级 `primary menu`，由 `/Admin/Site/Navigation` 读写 `workspace/site/navigation.yaml`；Theme 自行决定桌面端、移动端和嵌套展示。
- `site/navigation.yaml` 和 `navigation.json` 文件名保留，内容升级为正式 Menu tree；开发早期不为旧 `title/href` 扁平结构写兼容层。
- Menu target 固定为 `builtin`、`themePage`、`page`、`postCategory`；无法解析的 target 在 Dashboard 保留并警告，公开 Theme input 中跳过。
- Page template 由 Theme manifest 的 `pageTemplates[]` 声明，`normal` 强制存在；Page frontmatter 只保存 `template` name。
- Theme special page 由 Theme manifest 的 `specialPages[]` 声明，Home Server 不生成内容，只允许 Menu 指向其站点根相对 `route`。
- Post Category 增加稳定 `slug`，URL 固定为 `/posts/categories/{slug}/`；已有 slug 不随 rename 自动改变。

## 17. 实施记录

### 2026-05-16 M6-T01 / M6-T02 首轮切片

- 已新增 Dashboard JSON 资源目录 `Src/HomeServer/Bocchi.HomeServer/Localization/Dashboard/`，首批提供 `en-US` 与 `zh-CN`。
- 已新增 Dashboard UI language 的 culture cookie 端点，并在 Dashboard 顶栏与 `Settings / Localization` 使用紧凑下拉切换。该设置仍是后台 UI 偏好，不进入 Theme Contract。
- 已新增站点本地化数据库投影，保存 Site primary language、Site enabled languages、Custom languages 与固定 URL policy。
- 已在 `Settings / Localization` 放入基础 UI：Dashboard UI language、Site primary language、Site enabled languages、自定义语言添加/移除和 `PrimaryUnprefixed` 策略说明。
- 已补 Home Server 测试覆盖意图：站点主要语言自动保持启用、自定义语言保存、Dashboard UI language cookie 切换后的中文 Settings 渲染。
- 当时尚未把所有 Dashboard 页面文案迁移到 JSON，也尚未做可搜索 Language Picklist、Common i18n key 覆盖或 Theme Context `localization` 节点，因此 M6-T01 / M6-T02 先标记为进行中；后续闭环见下一段记录。

### 2026-05-16 M6-T01 / M6-T02 质量闭环与 M6-T03 最小切片

- 已清理上轮遗留的 MSBuild node 进程；`dotnet build-server shutdown` 未完全清掉后，手动终止残留 `MSBuild.dll /nodemode:1` 进程。随后单项目与完整 solution build 均未再复现 hang。
- HomeServer build blocker 实际落在 M6 首轮代码触发 analyzer-as-error：`DashboardLocalizationService` 的 `CA1848` 和本地化设置 helper 的 `CA1859`。已改为 `LoggerMessage` 委托和具体集合返回类型。
- `DashboardUiLanguageEndpoint_SetsCultureCookieAndRendersChineseSettings` 已改为先 HTML decode 再断言中文文案，匹配 Blazor prerender 会把非 ASCII 文案输出为 HTML entity 的实际行为。
- `Settings / Localization` 的 Language Picklist 已补最小可用体验：按 `code`、`nativeName`、`englishName` 搜索；已启用语言显示为 chips；添加自定义语言继续阻止重复；站点主要语言变更时立即保持 enabled，并在 UI 中 disabled 防误关。
- 通过 in-app Browser 在 `http://127.0.0.1:5081/Admin/Settings#localization` 验证：Dashboard UI language cookie 可渲染中文 Settings，Picklist 搜索 `ja` 会收窄到 `ja-JP`，主要语言切到 `en-US` 后会保持 checked/disabled，保存并 reload 后仍保留。
- Theme Context 已开始 M6-T03 的最小结构切片：`cache/theme-input/theme-context.json` 新增 `localization` 节点，当前只从 `site.yaml` 的 `language` 生成 `primaryLanguage`、单项 `enabledLanguages`、固定 `PrimaryUnprefixed` 和空 `text`。这一步刻意不把 HomeServer EF 设置硬耦进 Core Generator；Common i18n key 覆盖、Theme 私有 key 覆盖和完整站点 enabled languages 接入仍未完成。
- 验证：`dotnet build Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false`、`dotnet test Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false`、`dotnet test Tests/Bocchi.HomeServer.Tests/Bocchi.HomeServer.Tests.csproj --no-restore --disable-build-servers -v:minimal /m:1 /nr:false`、`dotnet test Tests/Bocchi.Generator.Tests/Bocchi.Generator.Tests.csproj --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 均通过。

### 2026-05-16 M6-T03 Common i18n 覆盖接入

- `Settings / Localization` 已新增 Common theme text 区块：内置 Common key 可搜索，按当前 Site enabled languages 展示 plain text 输入；空输入不持久化，保存后重新加载设置页状态。
- `LocalizationSettingsRecord` 已新增 `CommonTextOverridesJson`，并补手写 EF migration 与 model snapshot；`dotnet ef migrations has-pending-model-changes --no-build` 已确认没有未迁移模型差异。
- `LocalizationSettingsService` 现在可以把站点主要语言、启用语言、固定 `PrimaryUnprefixed` 与 Common text overrides 映射为 `BuildLocalizationOptions`。HomeServer 构建入口通过 scoped service 注入快照，Core Generator 保持不依赖 EF Core；CLI / 测试未传快照时继续回退到 `site.yaml` 单语言事实。
- `theme-context.json` 的 `localization` 节点已接入 HomeServer 快照：`enabledLanguages` 会包含设置页启用语言，`text` 会输出 Common i18n 覆盖。构建指纹已纳入 localization 快照，避免只改前台文案覆盖时被 up-to-date short-circuit 跳过。
- Theme manifest 已新增可选 `i18n` 声明，`default-static` manifest 已声明首批支持语言和两个私有 key 默认值；`theme-context.json` 会在 `theme.i18n` 中暴露这些声明，供 Theme 与后续 Dashboard 私有覆盖 UI 共用。
- 已补测试覆盖 Common 覆盖的清理/持久化/构建快照、Theme Context text 输出、localization text 变化会触发重新构建，以及 Theme manifest 私有 i18n key 声明。当时 Theme 私有 key 的用户覆盖存储和 `Settings / Theme` UI 尚未开始，因此 M6-T03 仍保持进行中；后续闭环见下一段记录。
- 验证：`dotnet build Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过；`dotnet test Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过 75 个测试；`dotnet test Tests/Bocchi.GeneratorContract.Tests/Bocchi.GeneratorContract.Tests.csproj --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过 6 个测试；`dotnet test Tests/Bocchi.Generator.Tests/Bocchi.Generator.Tests.csproj --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过 18 个测试；`git diff --check` 通过。最终进程检查未发现残留 `MSBuild.dll` 或 `Bocchi.HomeServer` 进程。

### 2026-05-16 M6-T03 Theme 私有覆盖闭环

- `ThemeConfigurationRecord` 已新增 `I18nTextOverridesJson`，按 Theme id 隔离保存 Theme 私有 i18n key 覆盖；已补手写 EF migration 与 model snapshot。
- `Settings / Theme` 已读取当前 Theme manifest 的 `i18n.keys`，按站点启用语言展示 Theme 私有文本输入。输入框 placeholder 使用 manifest default values，空输入不保存，保存后重新加载覆盖状态。
- HomeServer 构建入口会读取当前 Theme 的私有覆盖并写入 `BuildLocalizationOptions.ThemeTextOverrides`。Generator 合并 `localization.text` 时先写 Common 覆盖，再写 Theme 私有覆盖，因此同 key + language 下 Theme 私有覆盖优先。
- `theme-context.json` 现在同时包含 `localization.text` 中的用户覆盖，以及 `theme.i18n.keys[].defaultValues` 中的 Theme 默认值。Theme 可以按“Theme 用户覆盖 → Common 用户覆盖 → manifest 默认值 → key 本身”的顺序解析。
- 已补测试覆盖 Theme 私有覆盖保存/清理/构建快照、Settings 页面渲染 Theme 私有 key、Theme 私有覆盖优先级，以及 Theme 私有覆盖变化触发重新构建。M6-T03 按当前验收标准收束为完成；默认 Theme 实际消费这些文案属于 M6-T07。
- 验证：`dotnet ef migrations has-pending-model-changes --project Src/HomeServer/Bocchi.HomeServer/Bocchi.HomeServer.csproj --startup-project Src/HomeServer/Bocchi.HomeServer/Bocchi.HomeServer.csproj --no-build` 通过；`dotnet build Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过；`dotnet test Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false` 通过 78 个测试；`git diff --check` 通过。Playwright CLI 因本机未缓存 `@playwright/cli` 且当前环境无网络无法做浏览器截图，已改用 HomeServer smoke 测试覆盖 Settings / Theme 渲染，并确认没有残留 `MSBuild.dll`、`Bocchi.HomeServer` 或 Playwright CLI 进程。

### 2026-05-23 M6-T10 Frontend Menu v1 与 Theme Page Contract

- 已把 ContentModel 的 navigation 升级为正式 Menu tree：节点包含 `id`、`label`、`target`、`children`，嵌套最大深度沿用 Category tree 的 5 层。
- 已新增 `ThemeManifest.pageTemplates`、`ThemeManifest.specialPages`，Dashboard 可按当前 active Theme 展示 Page template 下拉，并对 unavailable template / theme page 给出保留原值的警告。
- 已新增 Post Category `slug` 编辑、归一化和同树唯一性校验；Generator 输出 `post-categories.json`，并在 `posts.json` 中输出 `categorySlug`。
- 已替换 `/Admin/Site/Navigation` 占位页为正式 Menu editor，支持添加根项、添加子项、移动顺序、删除、选择 target、编辑 label 和保存。
- 已让默认 Theme 改为消费 `navigation.json`，递归渲染桌面与移动 Menu，并输出 Post Category 列表页；Page 渲染优先查找 `standalone-page-{template}.liquid`，缺失时回退 `standalone-page.liquid`。
- 已补 Workspace、GeneratorContract、Generator、HomeServer 测试覆盖。验证通过：四个目标测试项目、完整 `dotnet test Bocchi.slnx --no-restore --disable-build-servers -v:minimal /m:1 /nr:false`、`git diff --check`、Dashboard localization JSON `jq empty`。
- 浏览器 smoke 使用临时 DataRoot 完成 Setup 后验证：`/Admin/Site/Navigation`、Page editor template selector、Preview root、`/posts/categories/smoke/` 和 `412x915` 移动端导航均可打开。现有 `.bocchi-dev-data` 中的旧 default-static 实例仍保留旧 runner manifest，因此不作为本轮 smoke 的事实来源。
